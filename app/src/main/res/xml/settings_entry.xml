<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <CheckBoxPreference android:key="appTheme"
        android:summaryOn="Dark Theme is Active"
        android:summaryOff="Light Theme is Active"
        android:title="App Theme" />
    <Preference
        android:key="static_field"
        android:selectable="false"
        android:persistent="false"
        android:summary="You can always switch between themes while reading by using the toggle button on top" ></Preference>
    <Preference
        android:key="static_field"
        android:selectable="false"
        android:persistent="false"
        android:title="About Stories App"
        android:summary="App created by Jude Osbert K. In case of any suggestions or bugs contact me on 'judeosby@gmail.com'. Happy reading"></Preference>
</PreferenceScreen>
